---
phase: 01-run-contract
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - docs/contracts/run-artifact-layout.md
  - docs/contracts/replay-resume-contract.md
  - docs/examples/run-directory-example.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ORCH-02
user_setup: []
must_haves:
  truths:
    - "每个 run 都有独立目录，且顶层文件与子目录固定"
    - "用户可以通过 run_id 定位目录并判断当前是否可续跑或可重放"
    - "重放和续跑的语义明确复用同一个 run，而不是新建 run"
  artifacts:
    - path: "docs/contracts/run-artifact-layout.md"
      provides: "run 目录布局契约"
      contains: "snapshots"
    - path: "docs/contracts/replay-resume-contract.md"
      provides: "replay / resume 操作契约"
      contains: "same run_id"
    - path: "docs/examples/run-directory-example.md"
      provides: "完整 run 目录示例"
      contains: "manifest.json"
  key_links:
    - from: "docs/contracts/run-artifact-layout.md"
      to: "docs/contracts/replay-resume-contract.md"
      via: "resume 和 replay 依赖相同目录结构"
      pattern: "status.json|report.md"
    - from: "docs/examples/run-directory-example.md"
      to: "docs/contracts/run-artifact-layout.md"
      via: "示例与正式目录契约一致"
      pattern: "snapshots|decisions|execution|reports|meta"
---

<objective>
把 run 的目录组织、顶层文件、续跑与重放操作语义统一成可执行规则。

Purpose: 让后续 phases 可以围绕固定 artifact layout 写快照、决策、执行和报告，而不用重新发明 run 容器。
Output: artifact layout 契约、replay/resume 契约、完整目录示例，并把 STATE 的下一步路由与这些契约对齐。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-run-contract/01-CONTEXT.md
@.planning/phases/01-run-contract/01-01-SUMMARY.md
@.planning/phases/01-run-contract/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical artifact layout</name>
  <files>docs/contracts/run-artifact-layout.md, docs/examples/run-directory-example.md</files>
  <read_first>.planning/phases/01-run-contract/01-CONTEXT.md, docs/contracts/run-lifecycle.md, docs/contracts/run-manifest-schema.md, docs/contracts/run-status-schema.md</read_first>
  <action>Create `docs/contracts/run-artifact-layout.md` with a canonical directory tree for a run. The top level must contain exactly `manifest.json`, `status.json`, and `report.md` plus the fixed directories `snapshots`, `decisions`, `execution`, `reports`, and `meta`. Document what belongs in each directory, including `snapshots/*.json|*.parquet`, `decisions/*.json`, `execution/ledger.*`, `reports/*.md`, and `meta/*.json`. Create `docs/examples/run-directory-example.md` with one full example tree that uses `run_id + 短业务摘要` in the directory name.</action>
  <verify>Get-Content docs/contracts/run-artifact-layout.md; Get-Content docs/examples/run-directory-example.md</verify>
  <acceptance_criteria>
    - `docs/contracts/run-artifact-layout.md` contains `manifest.json`, `status.json`, and `report.md`
    - `docs/contracts/run-artifact-layout.md` contains all five directory names
    - `docs/examples/run-directory-example.md` contains a tree with `snapshots/`, `decisions/`, `execution/`, `reports/`, and `meta/`
  </acceptance_criteria>
  <done>run 容器结构已经能作为后续所有 phase 的 artifact 写入基线。</done>
</task>

<task type="auto">
  <name>Task 2: Define replay and resume contract</name>
  <files>docs/contracts/replay-resume-contract.md</files>
  <read_first>.planning/phases/01-run-contract/01-CONTEXT.md, docs/contracts/run-lifecycle.md, docs/contracts/run-artifact-layout.md</read_first>
  <action>Create `docs/contracts/replay-resume-contract.md` and explicitly separate `resume` from `replay`. `resume` must mean continuing an incomplete run using the same `run_id`, existing `artifact_root`, and `status.json.resume_from`. `replay` must mean re-reading a completed or paused run's artifacts for inspection, comparison, or report regeneration without mutating business identity fields. Add a precondition checklist for each operation and an error checklist for invalid states such as missing `status.json`, mismatched `run_id`, or missing required artifact directories.</action>
  <verify>Get-Content docs/contracts/replay-resume-contract.md</verify>
  <acceptance_criteria>
    - `docs/contracts/replay-resume-contract.md` contains `resume`
    - `docs/contracts/replay-resume-contract.md` contains `replay`
    - `docs/contracts/replay-resume-contract.md` contains `same run_id`
    - `docs/contracts/replay-resume-contract.md` contains `resume_from`
  </acceptance_criteria>
  <done>续跑与重放的语义被清楚区分，且都建立在同一个 run 契约之上。</done>
</task>

<task type="auto">
  <name>Task 3: Route project state to artifact-driven execution</name>
  <files>.planning/STATE.md</files>
  <read_first>.planning/STATE.md, docs/contracts/run-artifact-layout.md, docs/contracts/replay-resume-contract.md</read_first>
  <action>Update `.planning/STATE.md` so the phase notes and immediate next step reference the canonical artifact layout and replay/resume contract as the baseline for Phase 2 onward. Add an explicit note that downstream phases must write outputs into the fixed run container rather than inventing phase-specific ad hoc paths.</action>
  <verify>Select-String -Path .planning/STATE.md -Pattern 'artifact layout|run container|resume|replay'</verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains `artifact layout` or `run container`
    - `.planning/STATE.md` contains `resume` and `replay`
  </acceptance_criteria>
  <done>项目状态文档已经把后续 phase 的写入和续跑行为路由到统一 artifact 契约。</done>
</task>

</tasks>

<threat_model>
- Threat: 后续 phases 自己发明目录结构，导致 run 工件不可统一读取。 Mitigation: 在 artifact layout 合同中固定顶层文件和五个子目录，并在 STATE.md 中把它作为下游基线。
- Threat: resume 和 replay 混用，导致历史 run 被意外覆盖。 Mitigation: 在 replay/resume 合同中把“resume = mutate same run, replay = inspect without changing identity”写成明确规则。
- Threat: 缺少顶层文件时仍被误判为可续跑。 Mitigation: 在 replay/resume 合同中增加缺失 `manifest.json`、`status.json`、固定目录时的阻断条件。
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `docs/contracts/run-artifact-layout.md` 固定了顶层文件与五个子目录
- [ ] `docs/contracts/replay-resume-contract.md` 明确区分了 replay 与 resume
- [ ] `.planning/STATE.md` 已把后续 phase 路由到统一 run container 约束
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- run 工件容器和续跑/重放语义被固定
- Phase 2 以后可以直接把数据和执行产物落入同一目录体系
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-contract/01-03-SUMMARY.md`
</output>
