---
phase: 01-run-contract
plan: 05
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified:
  - .agents/skills/backtest-orchestrator/SKILL.md
  - docs/contracts/run-start-contract.md
  - docs/contracts/run-lookup-contract.md
  - docs/contracts/replay-resume-contract.md
  - docs/examples/run-operation-sequences.md
autonomous: true
gap_closure: true
requirements:
  - ORCH-02
user_setup: []
must_haves:
  truths:
    - "backtest-orchestrator 的启动流程明确规定如何生成 run_id、artifact_root 和初始 manifest/status"
    - "系统存在按 run_id 定位既有 run 并决定 resume 或 replay 的统一查找流程"
    - "start、resume、replay 都有可执行的 skill 工作流序列，而不是只有抽象定义"
  artifacts:
    - path: ".agents/skills/backtest-orchestrator/SKILL.md"
      provides: "总控 skill 的运行操作契约"
      contains: "artifact_root"
    - path: "docs/contracts/run-start-contract.md"
      provides: "run 创建与初始文件写入流程"
      contains: "manifest.json"
    - path: "docs/contracts/run-lookup-contract.md"
      provides: "按 run_id 查找和选择操作的流程"
      contains: "lookup by run_id"
    - path: "docs/examples/run-operation-sequences.md"
      provides: "start / resume / replay 的端到端序列"
      contains: "resume"
  key_links:
    - from: "docs/contracts/run-start-contract.md"
      to: "docs/contracts/run-lookup-contract.md"
      via: "start 产出的 run_id 和 artifact_root 被 lookup 复用"
      pattern: "run_id|artifact_root"
    - from: "docs/contracts/run-lookup-contract.md"
      to: "docs/contracts/replay-resume-contract.md"
      via: "lookup 的状态判断决定 resume 或 replay"
      pattern: "resume_from|replay|resume"
---

<objective>
把 `run_id`、`artifact_root`、resume、replay 从静态合同推进成可执行的 skill 工作流，修补 ORCH-02 的可操作性缺口。

Purpose: 让 Phase 1 不只停留在“定义字段和目录”，而是真正定义总控 skill 如何创建一个 run、如何按 `run_id` 找到它、以及何时允许 `resume` 或 `replay`。
Output: 扩展后的 orchestrator skill、run start 合同、run lookup 合同、更新后的 replay/resume 合同和端到端操作序列示例。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-run-contract/01-CONTEXT.md
@.planning/phases/01-run-contract/01-VERIFICATION.md
@.planning/phases/01-run-contract/01-04-SUMMARY.md
@docs/contracts/run-lifecycle.md
@docs/contracts/run-manifest-schema.md
@docs/contracts/run-status-schema.md
@docs/contracts/run-artifact-layout.md
@docs/contracts/replay-resume-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the run start workflow in the orchestrator contract</name>
  <files>.agents/skills/backtest-orchestrator/SKILL.md, docs/contracts/run-start-contract.md</files>
  <read_first>.agents/skills/backtest-orchestrator/SKILL.md, docs/contracts/run-manifest-schema.md, docs/contracts/run-status-schema.md, docs/contracts/run-artifact-layout.md, .planning/phases/01-run-contract/01-VERIFICATION.md</read_first>
  <action>Expand `.agents/skills/backtest-orchestrator/SKILL.md` so it names the exact start inputs `symbol`, `start`, `end`, `cadence`, and `strategy_profile`, then create `docs/contracts/run-start-contract.md` with an ordered workflow: validate inputs, generate a unique `run_id`, derive `artifact_root`, create the fixed run container, write initial `manifest.json`, write initial `status.json`, and transition the run from `created` to `ready`. Set the initial status contract to include `current_node=orchestrator`, `last_completed_node=null`, and `resume_from=data-node` once the run is ready for dispatch.</action>
  <verify>Get-Content .agents/skills/backtest-orchestrator/SKILL.md; Get-Content docs/contracts/run-start-contract.md</verify>
  <acceptance_criteria>
    - `.agents/skills/backtest-orchestrator/SKILL.md` contains `strategy_profile`
    - `.agents/skills/backtest-orchestrator/SKILL.md` contains `artifact_root`
    - `docs/contracts/run-start-contract.md` contains `manifest.json`
    - `docs/contracts/run-start-contract.md` contains `status.json`
    - `docs/contracts/run-start-contract.md` contains `resume_from=data-node`
  </acceptance_criteria>
  <done>总控 skill 已经明确知道如何启动并初始化一个真实 run，而不是只描述概念。</done>
</task>

<task type="auto">
  <name>Task 2: Define lookup-by-run_id and allowed operation selection</name>
  <files>docs/contracts/run-lookup-contract.md, docs/contracts/replay-resume-contract.md</files>
  <read_first>docs/contracts/run-start-contract.md, docs/contracts/run-manifest-schema.md, docs/contracts/run-status-schema.md, docs/contracts/replay-resume-contract.md, .planning/phases/01-run-contract/01-VERIFICATION.md</read_first>
  <action>Create `docs/contracts/run-lookup-contract.md` with an explicit `lookup by run_id` algorithm: accept `run_id`, resolve `artifact_root`, load `manifest.json` and `status.json`, verify the two files share the same `run_id`, inspect the current `state`, then decide which operations are legal. Update `docs/contracts/replay-resume-contract.md` so it references `run-lookup-contract.md` and adds an operation matrix covering `ready`, `paused`, `partial`, `completed`, `replay-ready`, and `failed`. Keep `resume_from` mandatory for resume and block resume or replay when `run_id` or required files do not match.</action>
  <verify>Get-Content docs/contracts/run-lookup-contract.md; Get-Content docs/contracts/replay-resume-contract.md</verify>
  <acceptance_criteria>
    - `docs/contracts/run-lookup-contract.md` contains `lookup by run_id`
    - `docs/contracts/run-lookup-contract.md` contains `artifact_root`
    - `docs/contracts/replay-resume-contract.md` contains `run-lookup-contract.md`
    - `docs/contracts/replay-resume-contract.md` contains `resume_from`
    - `docs/contracts/replay-resume-contract.md` contains `completed`, `paused`, `partial`, and `failed`
  </acceptance_criteria>
  <done>系统已经定义好如何按 run_id 找到既有 run，并基于状态安全地选择 resume 或 replay。</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end operation sequences for start, resume, and replay</name>
  <files>.agents/skills/backtest-orchestrator/SKILL.md, docs/examples/run-operation-sequences.md</files>
  <read_first>.agents/skills/backtest-orchestrator/SKILL.md, docs/contracts/run-start-contract.md, docs/contracts/run-lookup-contract.md, docs/contracts/replay-resume-contract.md</read_first>
  <action>Add an `Operation Sequences` section to `.agents/skills/backtest-orchestrator/SKILL.md` that points to `docs/examples/run-operation-sequences.md`. Create `docs/examples/run-operation-sequences.md` with three explicit sequences named `start new run`, `resume paused run`, and `replay existing run`. Each sequence must list inputs, file reads or writes, required state transitions, and the expected outcome using concrete terms such as `run_id`, `artifact_root`, `manifest.json`, `status.json`, `resume_from`, and `report.md`.</action>
  <verify>Get-Content .agents/skills/backtest-orchestrator/SKILL.md; Get-Content docs/examples/run-operation-sequences.md</verify>
  <acceptance_criteria>
    - `.agents/skills/backtest-orchestrator/SKILL.md` contains `Operation Sequences`
    - `.agents/skills/backtest-orchestrator/SKILL.md` contains `run-operation-sequences.md`
    - `docs/examples/run-operation-sequences.md` contains `start new run`
    - `docs/examples/run-operation-sequences.md` contains `resume paused run`
    - `docs/examples/run-operation-sequences.md` contains `replay existing run`
    - `docs/examples/run-operation-sequences.md` contains `run_id`
  </acceptance_criteria>
  <done>start、resume、replay 三条路径都被写成可执行的 skill 工作流序列。</done>
</task>

</tasks>

<threat_model>
- Threat: run start 仍然只有字段定义，没有实际初始化顺序。 Mitigation: 在 orchestrator skill 和 `run-start-contract.md` 中写死输入、目录创建、文件写入和初始状态迁移顺序。
- Threat: replay 和 resume 在查找阶段混用，导致读取错目录或覆盖历史 run。 Mitigation: 新增 `run-lookup-contract.md`，要求先按 `run_id` 定位并校验 `artifact_root`、`manifest.json`、`status.json` 一致性。
- Threat: `failed`、`completed`、`paused` 等状态的允许操作不清楚。 Mitigation: 在 `replay-resume-contract.md` 中增加状态矩阵，并把 `resume_from` 的要求写成显式阻断条件。
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `backtest-orchestrator` 已经定义 run start 的初始化顺序
- [ ] `docs/contracts/run-lookup-contract.md` 已定义按 `run_id` 定位既有 run 的流程
- [ ] `docs/examples/run-operation-sequences.md` 已覆盖 `start new run`、`resume paused run`、`replay existing run`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `run_id`、`artifact_root`、resume、replay 不再只是抽象合同，而是具备可执行的 skill 工作流定义
- 验证报告中的 ORCH-02 blocker 被转化为明确的后续可执行基线
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-contract/01-05-SUMMARY.md`
</output>
