---
phase: 01-run-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/backtest-orchestrator/SKILL.md
  - .agents/skills/data-node/SKILL.md
  - .agents/skills/brain-node/SKILL.md
  - .agents/skills/execution-node/SKILL.md
  - .agents/skills/report-node/SKILL.md
  - docs/contracts/skill-topology.md
  - AGENTS.md
autonomous: true
requirements:
  - ORCH-01
user_setup: []
must_haves:
  truths:
    - "项目存在单一总入口 skill，且文档明确其只负责调度"
    - "数据、研判、执行、复盘四类子 skill 的职责边界被单独定义"
    - "下游阶段可以直接从 skill 文档读取入口、调用顺序和边界约束"
  artifacts:
    - path: ".agents/skills/backtest-orchestrator/SKILL.md"
      provides: "总控调度入口 skill 契约"
      contains: "纯调度器"
    - path: "docs/contracts/skill-topology.md"
      provides: "总控与四类子 skill 的拓扑说明"
      contains: "数据"
    - path: "AGENTS.md"
      provides: "项目级协作约束与 skill-first 路线说明"
      contains: "总控"
  key_links:
    - from: ".agents/skills/backtest-orchestrator/SKILL.md"
      to: "docs/contracts/skill-topology.md"
      via: "职责与调用拓扑一致"
      pattern: "数据|研判|执行|复盘"
    - from: "AGENTS.md"
      to: ".agents/skills/backtest-orchestrator/SKILL.md"
      via: "项目指令引用总控入口"
      pattern: "backtest-orchestrator|总控"
---

<objective>
定义项目的 skill-first 执行拓扑，把“单一总入口 + 四类子 skill”的结构固化成可执行约束。

Purpose: 让后续 phase 不会把系统做成零散命令集合或让顶层入口承担分析职责。
Output: 总控 skill 契约、四类子 skill 契约占位、拓扑说明文档和更新后的 AGENTS 约束。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-run-contract/01-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define orchestrator skill contract</name>
  <files>.agents/skills/backtest-orchestrator/SKILL.md, docs/contracts/skill-topology.md</files>
  <read_first>.planning/phases/01-run-contract/01-CONTEXT.md, .planning/ROADMAP.md, AGENTS.md</read_first>
  <action>Create `.agents/skills/backtest-orchestrator/SKILL.md` and define the total entry skill as a pure orchestrator. The document must explicitly say that the top-level skill receives a backtest request, creates or resolves a `run`, delegates work to `data-node`, `brain-node`, `execution-node`, and `report-node`, and never performs analysis itself. In `docs/contracts/skill-topology.md`, add a topology section listing the exact four node categories and the call order `总控 -> 数据 -> 研判 -> 执行 -> 复盘`.</action>
  <verify>Get-Content .agents/skills/backtest-orchestrator/SKILL.md; Get-Content docs/contracts/skill-topology.md</verify>
  <acceptance_criteria>
    - `.agents/skills/backtest-orchestrator/SKILL.md` contains `纯调度器`
    - `.agents/skills/backtest-orchestrator/SKILL.md` contains `data-node`, `brain-node`, `execution-node`, and `report-node`
    - `docs/contracts/skill-topology.md` contains `总控 -> 数据 -> 研判 -> 执行 -> 复盘`
  </acceptance_criteria>
  <done>总控入口被定义为纯调度器，且四类子 skill 的调用拓扑被明确记录。</done>
</task>

<task type="auto">
  <name>Task 2: Add node skill boundary documents</name>
  <files>.agents/skills/data-node/SKILL.md, .agents/skills/brain-node/SKILL.md, .agents/skills/execution-node/SKILL.md, .agents/skills/report-node/SKILL.md</files>
  <read_first>.planning/phases/01-run-contract/01-CONTEXT.md, docs/contracts/skill-topology.md</read_first>
  <action>Create four node skill docs under `.agents/skills/`. Each file must declare one node's single responsibility and forbidden responsibilities. `data-node` must own point-in-time snapshot assembly only. `brain-node` must own schema-validated decision generation only. `execution-node` must own run-state transition and ledger mutation only. `report-node` must own report and summary artifact generation only. Every file must include an explicit `Inputs` section and `Outputs` section so downstream planning can map contracts without ambiguity.</action>
  <verify>Get-ChildItem .agents/skills -Recurse -File</verify>
  <acceptance_criteria>
    - `.agents/skills/data-node/SKILL.md` contains `point-in-time` or `点时点`
    - `.agents/skills/brain-node/SKILL.md` contains `schema` and `JSON`
    - `.agents/skills/execution-node/SKILL.md` contains `run-state` or `状态`
    - `.agents/skills/report-node/SKILL.md` contains `report` and `artifact`
  </acceptance_criteria>
  <done>四类子 skill 的职责边界、输入和输出都被拆开定义。</done>
</task>

<task type="auto">
  <name>Task 3: Align AGENTS guidance to skill-first orchestration</name>
  <files>AGENTS.md</files>
  <read_first>AGENTS.md, .planning/phases/01-run-contract/01-CONTEXT.md, docs/contracts/skill-topology.md</read_first>
  <action>Update `AGENTS.md` so the repo-level guidance explicitly references the skill-first implementation path. Add a section stating that Phase 1 adopts a single orchestrator skill, that downstream work should preserve the four node boundaries, and that any future command surface or code wrapper must remain subordinate to the skill contracts defined in `.agents/skills/` and `docs/contracts/skill-topology.md`.</action>
  <verify>Select-String -Path AGENTS.md -Pattern 'skill|总控|data-node|brain-node|execution-node|report-node'</verify>
  <acceptance_criteria>
    - `AGENTS.md` contains `.agents/skills/`
    - `AGENTS.md` contains `single orchestrator skill` or `总控 skill`
    - `AGENTS.md` contains all four node names
  </acceptance_criteria>
  <done>项目级说明与 skill-first 拓扑保持一致，后续 phases 不会偏离这一结构。</done>
</task>

</tasks>

<threat_model>
- Threat: 顶层入口重新承担分析职责，导致边界坍缩。 Mitigation: 在总控 skill 文档和 AGENTS.md 中显式禁止 orchestrator 直接生成策略判断。
- Threat: 子 skill 职责交叉，后续 phases 重复写相同逻辑。 Mitigation: 每个 node skill 文档都包含 single responsibility、forbidden responsibilities、inputs、outputs。
- Threat: 后续实现绕过 skill 契约直接以零散命令替代。 Mitigation: 在 skill topology 文档里把总控入口和四节点关系设为项目级约束。
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `.agents/skills/backtest-orchestrator/SKILL.md` exists and defines the total entry contract
- [ ] 四个 node skill 文档都存在并包含 Inputs / Outputs
- [ ] `AGENTS.md` 与 `docs/contracts/skill-topology.md` 的边界描述一致
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 项目存在统一的 skill-first 入口拓扑文档
- 后续 planning 可以基于这些 skill 契约继续拆 phase
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-contract/01-01-SUMMARY.md`
</output>
