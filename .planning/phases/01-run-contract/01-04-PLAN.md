---
phase: 01-run-contract
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/examples/skill-entry-examples.md
autonomous: true
gap_closure: true
requirements:
  - ORCH-01
user_setup: []
must_haves:
  truths:
    - "Phase 1 的入口表述与 skill-first 决策一致，不再把 CLI 作为唯一入口真相"
    - "ORCH-01 明确允许通过 backtest-orchestrator 发起 run，并把 CLI wrapper 定位为可选从属层"
    - "用户可以读取单一示例文档来理解 start、resume 和 replay 的 skill 入口形态"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "对 ORCH-01 的 skill-first 需求表述"
      contains: "backtest-orchestrator"
    - path: ".planning/ROADMAP.md"
      provides: "与 skill-first 现实一致的 Phase 1 成功标准"
      contains: "wrapper"
    - path: "docs/examples/skill-entry-examples.md"
      provides: "总控 skill 的用户入口示例"
      contains: "resume"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "项目叙事与 ORCH-01 入口表述一致"
      pattern: "skill-first|backtest-orchestrator"
    - from: "docs/examples/skill-entry-examples.md"
      to: ".agents/skills/backtest-orchestrator/SKILL.md"
      via: "示例引用正式总控入口"
      pattern: "backtest-orchestrator|start|resume|replay"
---

<objective>
修补 Phase 1 的入口表述缺口，把 roadmap 和 requirements 从“CLI-first wording”对齐到当前已经锁定的 skill-first 实施现实。

Purpose: 消除验证报告里“文档说 CLI，实际走 skill”这一 blocker，让 ORCH-01 的验收口径与用户明确选择的实现路径一致。
Output: 更新后的 `PROJECT.md`、`REQUIREMENTS.md`、`ROADMAP.md`，以及一个可直接引用的 skill 入口示例文档。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-run-contract/01-CONTEXT.md
@.planning/phases/01-run-contract/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align project narrative and ORCH-01 wording to skill-first entry</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, AGENTS.md, .planning/phases/01-run-contract/01-VERIFICATION.md</read_first>
  <action>Update `.planning/PROJECT.md` so the repository is described as `skill-first` rather than `CLI-first`, while explicitly preserving the idea that a future CLI wrapper may exist as a subordinate surface. Update `.planning/REQUIREMENTS.md` so `ORCH-01` says a researcher can start a backtest through `backtest-orchestrator` or a future wrapper that delegates to it. Keep the required inputs explicit as `symbol`, `start`, `end`, `cadence`, and `strategy_profile`.</action>
  <verify>Get-Content .planning/PROJECT.md; Get-Content .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains `skill-first`
    - `.planning/PROJECT.md` contains `backtest-orchestrator`
    - `.planning/REQUIREMENTS.md` contains `ORCH-01`
    - `.planning/REQUIREMENTS.md` contains `strategy_profile`
    - `.planning/REQUIREMENTS.md` contains `backtest-orchestrator` or `总控 skill`
  </acceptance_criteria>
  <done>项目叙事和 ORCH-01 不再与用户选择的 skill-first 路径冲突。</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Phase 1 roadmap success criteria around the orchestrator skill</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-run-contract/01-VERIFICATION.md</read_first>
  <action>Update the Phase 1 section in `.planning/ROADMAP.md` so success criterion 1 names `backtest-orchestrator` as the canonical entry surface instead of `backtest run`. Keep criteria 2 and 3 focused on `run_id`, artifact creation, and replay preparation, but phrase them as skill workflow expectations. Also add explicit wording that any future CLI wrapper must delegate to the orchestrator skill rather than replace it.</action>
  <verify>Get-Content .planning/ROADMAP.md</verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `backtest-orchestrator`
    - `.planning/ROADMAP.md` contains `run_id`
    - `.planning/ROADMAP.md` contains `wrapper`
    - `.planning/ROADMAP.md` contains `skill`
  </acceptance_criteria>
  <done>roadmap 的 Goal 和 Success Criteria 与 skill-first 路线保持一致。</done>
</task>

<task type="auto">
  <name>Task 3: Add user-facing skill entry examples for start, resume, and replay</name>
  <files>docs/examples/skill-entry-examples.md</files>
  <read_first>.agents/skills/backtest-orchestrator/SKILL.md, .planning/PROJECT.md, .planning/ROADMAP.md</read_first>
  <action>Create `docs/examples/skill-entry-examples.md` with three concrete sections named `start`, `resume`, and `replay`. In the `start` example, show a request shape that includes `symbol`, `start`, `end`, `cadence`, and `strategy_profile` and routes through `backtest-orchestrator`. In the `resume` and `replay` examples, show how the operator supplies `run_id` and what behavior is expected. Add a note that any future CLI wrapper is only a delegate to the same orchestrator contract.</action>
  <verify>Get-Content docs/examples/skill-entry-examples.md</verify>
  <acceptance_criteria>
    - `docs/examples/skill-entry-examples.md` contains `start`
    - `docs/examples/skill-entry-examples.md` contains `resume`
    - `docs/examples/skill-entry-examples.md` contains `replay`
    - `docs/examples/skill-entry-examples.md` contains `run_id`
    - `docs/examples/skill-entry-examples.md` contains `backtest-orchestrator`
  </acceptance_criteria>
  <done>用户可以通过单一示例文档理解 skill-first 入口而不必反推多份合同。</done>
</task>

</tasks>

<threat_model>
- Threat: PROJECT、REQUIREMENTS、ROADMAP 继续各说各话，导致 Phase 1 永远无法按同一标准验收。 Mitigation: 在三个核心文件里统一写明 `backtest-orchestrator` 是 canonical entry，CLI wrapper 仅为从属包装层。
- Threat: 入口改成 skill-first 后丢失关键启动参数。 Mitigation: 在 ORCH-01 和示例文档中保留 `symbol`、`start`、`end`、`cadence`、`strategy_profile` 的显式字段。
- Threat: 用户无法理解 start、resume、replay 的入口差异。 Mitigation: 新增 `docs/examples/skill-entry-examples.md`，用三个具体段落示例化入口行为。
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `PROJECT.md`、`REQUIREMENTS.md`、`ROADMAP.md` 的入口表述一致
- [ ] `ORCH-01` 明确允许通过 `backtest-orchestrator` 发起 run
- [ ] `docs/examples/skill-entry-examples.md` 覆盖了 `start`、`resume`、`replay`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 验证报告中的“CLI-first wording vs skill-first reality” blocker 被消除
- Phase 1 的入口口径与用户选择的 skill-first 路径一致
</success_criteria>

<output>
After completion, create `.planning/phases/01-run-contract/01-04-SUMMARY.md`
</output>
