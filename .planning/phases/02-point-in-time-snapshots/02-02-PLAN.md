---
phase: 02-point-in-time-snapshots
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contracts/run-artifact-layout.md
  - docs/contracts/snapshot-validation.md
autonomous: true
requirements:
  - DATA-02
  - DATA-04
user_setup: []
must_haves:
  truths:
    - "点时点约束必须是硬校验，不能静默修复未来函数"
    - "manual 输入的来源说明和验证结果必须能在 run container 中追溯"
    - "meta/ 下会保存验证信息和原始手工输入痕迹"
  artifacts:
    - path: "docs/contracts/run-artifact-layout.md"
      provides: "run container 的 metadata 子结构说明"
      contains: "manual-inputs"
    - path: "docs/contracts/snapshot-validation.md"
      provides: "快照验证和缓存复用规则"
      contains: "candle.date <= asof_date"
  key_links:
    - from: "docs/contracts/snapshot-validation.md"
      to: "docs/contracts/run-artifact-layout.md"
      via: "validation artifacts must live inside the fixed run container"
      pattern: "meta/validation.json|meta/manual-inputs/"
    - from: "docs/contracts/run-artifact-layout.md"
      to: "docs/contracts/point-in-time-snapshot.md"
      via: "layout must match snapshot contract filenames"
      pattern: "snapshots/|meta/"
---

<objective>
把快照验证、缓存复用和 run container 里的 metadata 归档规则定死。
Purpose: 让 Phase 2 的快照不仅能“写出来”，还要能“验得过、查得到、重放得回去”。 Output: run artifact layout 更新、snapshot validation contract、以及 validation 结果在 `meta/` 中的落盘规则。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-point-in-time-snapshots/02-CONTEXT.md
@.planning/phases/02-point-in-time-snapshots/02-DISCUSSION-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend run container metadata rules</name>
  <files>docs/contracts/run-artifact-layout.md</files>
  <read_first>.planning/phases/02-point-in-time-snapshots/02-CONTEXT.md, docs/contracts/run-artifact-layout.md</read_first>
  <action>Update `docs/contracts/run-artifact-layout.md` so `meta/` explicitly covers two Phase 2 subpaths: `meta/manual-inputs/` for raw operator-provided source payloads and `meta/validation.json` for point-in-time validation results. Keep the top-level run container unchanged, but clarify that the metadata subtree may contain raw inputs, validation records, and provenance files needed to audit snapshot assembly.</action>
  <verify>Get-Content docs/contracts/run-artifact-layout.md</verify>
  <acceptance_criteria>
    - `docs/contracts/run-artifact-layout.md` contains `meta/manual-inputs/`
    - `docs/contracts/run-artifact-layout.md` contains `meta/validation.json`
    - `docs/contracts/run-artifact-layout.md` still contains `snapshots/`, `decisions/`, `execution/`, `reports/`, and `meta/`
  </acceptance_criteria>
  <done>run container 的 metadata 子结构足以承载 Phase 2 的手工输入与验证记录。</done>
</task>

<task type="auto">
  <name>Task 2: Define snapshot validation contract</name>
  <files>docs/contracts/snapshot-validation.md</files>
  <read_first>.planning/phases/02-point-in-time-snapshots/02-CONTEXT.md, docs/contracts/point-in-time-snapshot.md, docs/contracts/run-artifact-layout.md</read_first>
  <action>Create `docs/contracts/snapshot-validation.md` with the validation rules for Phase 2. The contract must require `candle.date <= asof_date`, block any snapshot that violates point-in-time visibility, and record violations in `meta/validation.json`. It must also define cache reuse rules for identical input payloads and versioned parameters so standardized snapshots can be reused without changing the business identity of the run.</action>
  <verify>Get-Content docs/contracts/snapshot-validation.md</verify>
  <acceptance_criteria>
    - `docs/contracts/snapshot-validation.md` contains `candle.date <= asof_date`
    - `docs/contracts/snapshot-validation.md` contains `meta/validation.json`
    - `docs/contracts/snapshot-validation.md` contains `cache reuse`
    - `docs/contracts/snapshot-validation.md` contains `blocked` or `block`
  </acceptance_criteria>
  <done>快照校验和缓存复用规则已经可以被执行阶段直接引用。</done>
</task>

</tasks>

<threat_model>
- Threat: 未来函数被写进快照后没有被拦截。 Mitigation: validation contract hard-blocks any `candle.date > asof_date` case and persists the violation.
- Threat: 原始手工输入与验证结果散落到不同位置，审计时找不到。 Mitigation: force both `meta/manual-inputs/` and `meta/validation.json` under the fixed run container.
- Threat: 缓存复用改变了快照语义。 Mitigation: reuse only when input payloads and versioned parameters are identical.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `docs/contracts/run-artifact-layout.md` explicitly names `meta/manual-inputs/`
- [ ] `docs/contracts/snapshot-validation.md` blocks post-dated candles
- [ ] `docs/contracts/snapshot-validation.md` records violations in `meta/validation.json`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a concrete validation and provenance story
- later phases can trust the snapshot layer without re-checking ad hoc rules
</success_criteria>

<output>
After completion, create `.planning/phases/02-point-in-time-snapshots/02-02-SUMMARY.md`
</output>
