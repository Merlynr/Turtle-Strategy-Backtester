---
phase: 02-point-in-time-snapshots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contracts/point-in-time-snapshot.md
  - .agents/skills/data-node/SKILL.md
autonomous: true
requirements:
  - DATA-01
  - DATA-03
user_setup: []
must_haves:
  truths:
    - "manual provider 是 Phase 2 的主路径，不需要 Tushare 或 AKShare 凭证"
    - "每个调仓时点的快照以 asof_date 锚定，并由 prices.csv 和指标输入共同构成"
    - "data-node 只负责快照装配，不负责买卖决策或账本"
  artifacts:
    - path: "docs/contracts/point-in-time-snapshot.md"
      provides: "点时点快照输入/输出契约"
      contains: "snapshot_{asof_date}.json"
    - path: ".agents/skills/data-node/SKILL.md"
      provides: "data-node 职责与输入输出边界"
      contains: "point-in-time snapshots"
  key_links:
    - from: "docs/contracts/point-in-time-snapshot.md"
      to: ".agents/skills/data-node/SKILL.md"
      via: "快照契约驱动 node 职责"
      pattern: "manual|asof_date|normalized snapshot"
    - from: "docs/contracts/point-in-time-snapshot.md"
      to: "docs/contracts/run-artifact-layout.md"
      via: "快照文件必须落入固定 run container"
      pattern: "snapshots/|meta/"
---

<objective>
把“手工输入 K 线和指标 -> 标准化快照 -> 交给 brain-node”这条主路径定成可执行契约。
Purpose: 后续执行阶段只要读取同一份快照契约，就能把操作者输入固化为稳定工件，而不是每次临时拼 JSON。 Output: point-in-time snapshot contract、更新后的 data-node skill、以及明确的输入/输出边界。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-point-in-time-snapshots/02-CONTEXT.md
@.planning/phases/02-point-in-time-snapshots/02-DISCUSSION-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define manual snapshot contract</name>
  <files>docs/contracts/point-in-time-snapshot.md, .agents/skills/data-node/SKILL.md</files>
  <read_first>.planning/phases/02-point-in-time-snapshots/02-CONTEXT.md, docs/contracts/run-artifact-layout.md, .agents/skills/data-node/SKILL.md</read_first>
  <action>Create `docs/contracts/point-in-time-snapshot.md` and define the manual snapshot contract. The document must state that v1 accepts `provider=manual`, `symbol`, `market`, `cadence`, `asof_date`, `prices.csv`, and operator-supplied indicators/notes. It must define the canonical snapshot filename as `snapshot_{asof_date}.json`, require the file to live under `{artifact_root}/snapshots/`, and describe the normalized snapshot fields that `brain-node` will consume. Update `.agents/skills/data-node/SKILL.md` so the node's Inputs and Outputs exactly match the new snapshot contract and still make clear that data-node only assembles point-in-time snapshots.</action>
  <verify>Get-Content docs/contracts/point-in-time-snapshot.md; Get-Content .agents/skills/data-node/SKILL.md</verify>
  <acceptance_criteria>
    - `docs/contracts/point-in-time-snapshot.md` contains `provider=manual`
    - `docs/contracts/point-in-time-snapshot.md` contains `snapshot_{asof_date}.json`
    - `docs/contracts/point-in-time-snapshot.md` contains `prices.csv`
    - `.agents/skills/data-node/SKILL.md` contains `point-in-time snapshots`
    - `.agents/skills/data-node/SKILL.md` contains `Inputs` and `Outputs`
  </acceptance_criteria>
  <done>点时点快照的输入、文件名和 data-node 职责边界已被固定为统一契约。</done>
</task>

</tasks>

<threat_model>
- Threat: 手工输入和标准化快照混在一起，导致后续无法重放同一时点。 Mitigation: 在快照契约里强制区分 `prices.csv` 原始输入和 `snapshot_{asof_date}.json` 标准化输出。
- Threat: data-node 越权生成决策或账本。 Mitigation: 在 skill 文档里保留单一职责和禁止职责。
- Threat: 不同执行轮次把快照写成临时文件名。 Mitigation: 规定固定命名 `snapshot_{asof_date}.json` 并写入 `{artifact_root}/snapshots/`。
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `docs/contracts/point-in-time-snapshot.md` 说明了 manual provider
- [ ] `docs/contracts/point-in-time-snapshot.md` 固定了 `snapshot_{asof_date}.json`
- [ ] `.agents/skills/data-node/SKILL.md` 仍然只负责快照装配
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 can express manual input data without external credentials
- Later phases can consume the same snapshot file shape without ad hoc parsing
</success_criteria>

<output>
After completion, create `.planning/phases/02-point-in-time-snapshots/02-01-SUMMARY.md`
</output>
