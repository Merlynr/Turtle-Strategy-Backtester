---
phase: 02-point-in-time-snapshots
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - docs/examples/point-in-time-snapshot-example.md
  - docs/examples/run-directory-example.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
user_setup: []
must_haves:
  truths:
    - "用户可以按一个固定模板提供价格序列、指标和备注"
    - "示例必须展示 snapshot 文件和 meta 验证记录如何一起落盘"
    - "示例必须让后续执行阶段无需猜测输入格式"
  artifacts:
    - path: "docs/examples/point-in-time-snapshot-example.md"
      provides: "手工输入与快照 JSON 示例"
      contains: "asof_date"
    - path: "docs/examples/run-directory-example.md"
      provides: "包含 Phase 2 产物的完整 run 树示例"
      contains: "meta/manual-inputs"
  key_links:
    - from: "docs/examples/point-in-time-snapshot-example.md"
      to: "docs/contracts/point-in-time-snapshot.md"
      via: "example must match contract fields"
      pattern: "prices.csv|snapshot_{asof_date}.json"
    - from: "docs/examples/run-directory-example.md"
      to: "docs/contracts/run-artifact-layout.md"
      via: "tree must match run container contract"
      pattern: "snapshots/|meta/manual-inputs/|meta/validation.json"
---

<objective>
把 Phase 2 的输入模板和 run 树示意写成可以直接照着操作的示例。
Purpose: 让操作者知道该怎么准备 `prices.csv`、`snapshot_{asof_date}.json`、以及 `meta/` 下的审计材料。 Output: 一个手工输入示例和一个完整 run 目录示例。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-point-in-time-snapshots/02-CONTEXT.md
@.planning/phases/02-point-in-time-snapshots/02-DISCUSSION-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add point-in-time example</name>
  <files>docs/examples/point-in-time-snapshot-example.md</files>
  <read_first>.planning/phases/02-point-in-time-snapshots/02-CONTEXT.md, docs/contracts/point-in-time-snapshot.md, docs/contracts/snapshot-validation.md</read_first>
  <action>Create `docs/examples/point-in-time-snapshot-example.md` that shows a complete manual input set: one `prices.csv` excerpt, one `snapshot_2026-04-23.json` example, and a short note explaining how `asof_date`, `indicators`, and `notes` are captured. Include at least one invalid example row or note that demonstrates how a post-dated candle is rejected by the validation contract.</action>
  <verify>Get-Content docs/examples/point-in-time-snapshot-example.md</verify>
  <acceptance_criteria>
    - `docs/examples/point-in-time-snapshot-example.md` contains `prices.csv`
    - `docs/examples/point-in-time-snapshot-example.md` contains `snapshot_2026-04-23.json`
    - `docs/examples/point-in-time-snapshot-example.md` contains `asof_date`
    - `docs/examples/point-in-time-snapshot-example.md` contains a rejection note or invalid example
  </acceptance_criteria>
  <done>操作者可以按示例准备一组可直接执行的点时点输入。</done>
</task>

<task type="auto">
  <name>Task 2: Add run tree example for Phase 2</name>
  <files>docs/examples/run-directory-example.md</files>
  <read_first>.planning/phases/02-point-in-time-snapshots/02-CONTEXT.md, docs/contracts/run-artifact-layout.md, docs/examples/run-directory-example.md</read_first>
  <action>Update `docs/examples/run-directory-example.md` so the example tree reflects a Phase 2 run that includes `snapshots/prices.csv`, at least one `snapshots/snapshot_{asof_date}.json`, `meta/manual-inputs/`, and `meta/validation.json` while keeping the Phase 1 top-level run container intact. The example should make it obvious where a human-provided source note, validation artifact, and normalized snapshot each live.</action>
  <verify>Get-Content docs/examples/run-directory-example.md</verify>
  <acceptance_criteria>
    - `docs/examples/run-directory-example.md` contains `snapshots/prices.csv`
    - `docs/examples/run-directory-example.md` contains `snapshots/snapshot_`
    - `docs/examples/run-directory-example.md` contains `meta/manual-inputs/`
    - `docs/examples/run-directory-example.md` contains `meta/validation.json`
  </acceptance_criteria>
  <done>Phase 2 的 run 树示意与 artifact layout 及快照示例保持一致。</done>
</task>

</tasks>

<threat_model>
- Threat: 示例和合同使用不同字段名，导致操作者照抄后无法执行。 Mitigation: example files must mirror the exact field names from the contract docs.
- Threat: 目录树示例遗漏 `meta/manual-inputs/` 或 `meta/validation.json`，让审计路径不明显。 Mitigation: include both in the example tree and keep top-level run container unchanged.
- Threat: 示例过于抽象，无法指导实际输入。 Mitigation: include concrete dates, filenames, and at least one invalid case.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `docs/examples/point-in-time-snapshot-example.md` shows valid and invalid input
- [ ] `docs/examples/run-directory-example.md` shows Phase 2 metadata files
- [ ] Example filenames match the contract filenames exactly
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 examples are concrete enough for hands-on use
- Later phases can point to the examples as the canonical operator-facing format
</success_criteria>

<output>
After completion, create `.planning/phases/02-point-in-time-snapshots/02-03-SUMMARY.md`
</output>
